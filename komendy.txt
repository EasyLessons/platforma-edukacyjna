# Backend (FastAPI)
# Aktywacja środowiska, instalacja zależności, uruchomienie serwera
python -m venv .venv
.\\.venv\Scripts\Activate.ps1
pip install -r backend/requirements.txt

uvicorn main:app --reload z glownego lub z /backend

# Frontend (Next.js)
# Instalacja zależności, uruchomienie serwera developerskiego
npm install
npm run dev

# alembic push do tabli zeby w oddzilencyh plikach robić tableki
